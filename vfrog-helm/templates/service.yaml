apiVersion: v1
kind: Service
metadata:
  name: {{ include "vfrog_helm.fullname" . }}
  labels:
    {{- include "vfrog_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vfrog_helm.selectorLabels" . | nindent 4 }}




